@page
@model RazorPagesCurrency.Pages_Currency.DetailsModel

@{
    ViewData["Name"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Currency</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Currency.Id)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Currency.Id)
        </dd>

        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Currency.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Currency.Name)
        </dd>

        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Currency.ExchangeRate)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Currency.ExchangeRate)
        </dd>

        <dt class="col-sm-2">
            Exchange Rate History
        </dt>
        <dd class="col-sm-10">
            <ul>
                @* @if (Model.Currency.ListExchange != null && Model.Currency.ListExchange.Count > 0)
                {
                    @foreach (var rate in Model.Currency.ListExchange)
                    {
                        <li>@rate.dateTime - @rate.ExchangeRateElement.ToString()</li>
                    }
                }
                else
                {
                    <p>Sorry, the currency details are not available.</p>
                } *@
            </ul>
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.Currency?.Id">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
